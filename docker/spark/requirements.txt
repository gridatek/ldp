# Core Spark dependencies
pyspark==4.0.1

# Data processing
pandas==2.3.3
numpy==2.3.5
pyarrow==22.0.0

# S3/MinIO support
boto3>=1.37.2
s3fs==2024.12.0
fsspec==2024.12.0

# Iceberg support (Python side)
# PyIceberg 0.10.0 - enhanced Iceberg table operations
# Note: May require fsspec version adjustments if compatibility issues arise
pyiceberg[s3fs]==0.10.0

# Utilities
pyyaml==6.0.1
python-dateutil==2.8.2
